OctoFit Tracker — Frontend Scaffold
===================================

This directory will contain the frontend application for OctoFit Tracker.

Recommended quick scaffold (Create React App):

```bash
npx create-react-app octofit-tracker/frontend
cd octofit-tracker/frontend
npm start
```

Or use your preferred frontend framework (Vite, Next.js, etc.). The project expects the frontend dev server to run on port `3000` by default.
